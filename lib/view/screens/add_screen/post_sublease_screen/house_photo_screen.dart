import 'package:flutter/material.dart';

class HousePhotoScreen extends StatelessWidget {
  const HousePhotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}