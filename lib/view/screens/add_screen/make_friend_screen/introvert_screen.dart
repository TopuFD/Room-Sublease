import 'package:flutter/widgets.dart';

class IntrovertScreen extends StatelessWidget {
  const IntrovertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}